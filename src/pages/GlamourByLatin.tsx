import SalonPage from "@/components/salon/SalonPage";
import { glamourByLatinData } from "@/data/glamourByLatin";

export default function GlamourByLatin() {
  return <SalonPage data={glamourByLatinData} />;
}
