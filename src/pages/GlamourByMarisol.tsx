import SalonPage from "@/components/salon/SalonPage";
import { glamourByMarisolData } from "@/data/glamourByMarisol";

export default function GlamourByMarisol() {
  return <SalonPage data={glamourByMarisolData} />;
}
